#
# Automatically generated file; DO NOT EDIT.
# OpenWrt Configuration
#

## Target Platform
CONFIG_TARGET_qualcommax=y
CONFIG_TARGET_qualcommax_ipq807x=y
CONFIG_TARGET_qualcommax_ipq807x_DEVICE_xiaomi_ax3600=y
#CONFIG_ATH11K_MEM_PROFILE_512M=y
#CONFIG_IPQ_MEM_PROFILE_512=y
#CONFIG_KERNEL_IPQ_MEM_PROFILE=512

CONFIG_HAS_SUBTARGETS=y
# (目标平台有子平台)
CONFIG_HAS_DEVICES=y
# (目标平台有特定设备型号)
CONFIG_TARGET_BOARD="qualcommax"
# (目标板名称)
CONFIG_TARGET_SUBTARGET="ipq807x"
# (目标子平台名称)
CONFIG_TARGET_PROFILE="DEVICE_xiaomi_ax3600"
# (目标配置文件名称，指向小米 AX3600)
CONFIG_TARGET_ARCH_PACKAGES="aarch64_cortex-a53"
# (目标架构软件包，适用于 aarch64 Cortex-A53 架构)
CONFIG_DEFAULT_TARGET_OPTIMIZATION="-Os -pipe -mcpu=cortex-a53"
# (默认的目标优化选项：-Os 表示优化大小，-pipe 使用管道而不是临时文件，-mcpu 指定 CPU 类型)
CONFIG_CPU_TYPE="cortex-a53"
# (CPU 类型为 Cortex-A53)
CONFIG_LINUX_6_12=y
# (使用的 Linux 内核版本为 6.12，实际版本可能根据具体 OpenWrt 版本而定，这里显示的是配置项)

#
# Default packages for qualcommax/ipq807x (小米 AX3600 相关的默认软件包)
#
CONFIG_DEFAULT_ath10k-firmware-qca9887-ct=y
# (默认包含 ath10k QCA9887 CT 固件，AX3600 的 IoT AIoT 天线可能使用此系列芯片或类似)
CONFIG_DEFAULT_ath11k-firmware-ipq8074=y
# (默认包含 ath11k IPQ8074 固件，这是 AX3600 Wi-Fi 6 (802.11ax) 功能的核心驱动固件)
CONFIG_DEFAULT_base-files=y
# (默认包含基础系统文件)
CONFIG_DEFAULT_ca-bundle=y
# (默认包含 CA 证书包，用于 HTTPS 等)
CONFIG_DEFAULT_dnsmasq=y
# (默认包含 dnsmasq，一个轻量级的 DNS、DHCP 和 TFTP 服务器)
CONFIG_DEFAULT_dropbear=y
# (默认包含 dropbear，一个轻量级的 SSH 服务器和客户端)
CONFIG_DEFAULT_e2fsprogs=y
# (默认包含 e2fsprogs，用于 ext2/ext3/ext4 文件系统的工具)
CONFIG_DEFAULT_firewall4=y
# (默认包含 firewall4，基于 nftables 的防火墙)
CONFIG_DEFAULT_fstools=y
# (默认包含 fstools，文件系统工具集)
CONFIG_DEFAULT_ipq-wifi-xiaomi_ax3600=y
# (默认包含小米 AX3600 特定的 Wi-Fi 配置/脚本)
CONFIG_DEFAULT_iwinfo=y
# (默认包含 iwinfo，用于获取无线信息的命令行工具)
CONFIG_DEFAULT_kmod-ath10k-ct-smallbuffers=y
# (默认包含 ath10k-ct 内核模块，使用小缓冲区版本)
CONFIG_DEFAULT_kmod-ath11k-ahb=y
# (默认包含 ath11k-ahb 内核模块，用于通过 AHB 总线连接的 ath11k 设备)
CONFIG_DEFAULT_kmod-fs-ext4=y
# (默认包含 ext4 文件系统内核模块)
CONFIG_DEFAULT_kmod-gpio-button-hotplug=y
# (默认包含 GPIO 按键热插拔内核模块)
CONFIG_DEFAULT_kmod-leds-gpio=y
# (默认包含 GPIO LED 控制内核模块)
CONFIG_DEFAULT_kmod-nft-offload=y
# (默认包含 nftables 硬件卸载内核模块，利用NSS等进行加速)
CONFIG_DEFAULT_kmod-phy-aquantia=y
# (默认包含 Aquantia PHY 内核模块，AX3600 的 2.5G 网口可能使用此 PHY)
CONFIG_DEFAULT_kmod-qca-nss-dp=y
# (默认包含 Qualcomm NSS Datapath 内核模块，用于网络加速)
CONFIG_DEFAULT_kmod-usb-dwc3=y
# (默认包含 DesignWare USB3 控制器内核模块)
CONFIG_DEFAULT_kmod-usb-dwc3-qcom=y
# (默认包含 Qualcomm 特定的 DesignWare USB3 控制器内核模块)
CONFIG_DEFAULT_kmod-usb3=y
# (默认包含 USB3 支持内核模块)
CONFIG_DEFAULT_libc=y
# (默认包含 C 标准库)
CONFIG_DEFAULT_libgcc=y
# (默认包含 GCC 运行时库)
CONFIG_DEFAULT_libustream-mbedtls=y
# (默认包含 ustream-mbedtls 库，用于 TLS/SSL)
CONFIG_DEFAULT_logd=y
# (默认包含 logd，日志守护进程)
CONFIG_DEFAULT_losetup=y
# (默认包含 losetup，用于设置和控制回环设备)
CONFIG_DEFAULT_mtd=y
# (默认包含 mtd，内存技术设备支持，用于闪存操作)
CONFIG_DEFAULT_netifd=y
# (默认包含 netifd，网络接口守护进程)
CONFIG_DEFAULT_nftables=y
# (默认包含 nftables 用户空间工具)
CONFIG_DEFAULT_odhcp6c=y
# (默认包含 odhcp6c，IPv6 DHCP 客户端)
CONFIG_DEFAULT_odhcpd-ipv6only=y
# (默认包含 odhcpd-ipv6only，仅 IPv6 的 DHCP 服务器)
CONFIG_DEFAULT_ppp=y
# (默认包含 ppp，点对点协议支持)
CONFIG_DEFAULT_ppp-mod-pppoe=y
# (默认包含 pppoe (PPP over Ethernet) 模块)
CONFIG_DEFAULT_procd-ujail=y
# (默认包含 procd 的 ujail 支持，用于进程沙箱)
CONFIG_DEFAULT_uboot-envtools=y
# (默认包含 U-Boot 环境变量工具)
CONFIG_DEFAULT_uci=y
# (默认包含 uci，OpenWrt 统一配置接口)
CONFIG_DEFAULT_uclient-fetch=y
# (默认包含 uclient-fetch，轻量级 URL 获取工具)
CONFIG_DEFAULT_urandom-seed=y
# (默认包含 urandom-seed，用于保存和恢复随机数种子)
CONFIG_DEFAULT_urngd=y
# (默认包含 urngd，用户空间随机数生成器守护进程)
CONFIG_DEFAULT_wpad-basic-mbedtls=y
# (默认包含 wpad-basic-mbedtls，基础的 WPA Supplicant 和 Host AP，使用 mbedTLS)

CONFIG_HAS_FPU=y
# (目标 CPU 有浮点运算单元)
CONFIG_AUDIO_SUPPORT=y
# (启用音频支持，AX3600 本身没有直接的音频输出，但平台可能支持)
CONFIG_GPIO_SUPPORT=y
# (启用 GPIO 支持)
CONFIG_PCI_SUPPORT=y
# (启用 PCI 总线支持)
CONFIG_PCIE_SUPPORT=y
# (启用 PCIe 总线支持，Wi-Fi 芯片通常通过 PCIe 连接)
CONFIG_USB_SUPPORT=y
# (启用 USB 支持，AX3600 有 USB 接口)
CONFIG_RTC_SUPPORT=y
# (启用实时时钟支持)
CONFIG_USES_DEVICETREE=y
# (使用设备树来描述硬件)
CONFIG_USES_INITRAMFS=y
# (使用 initramfs 作为初始根文件系统)
CONFIG_USES_SQUASHFS=y
# (固件使用 SquashFS 文件系统)
CONFIG_EMMC_SUPPORT=y
# (启用 eMMC 支持，AX3600 使用 eMMC 存储)
CONFIG_NAND_SUPPORT=y
# (启用 NAND 闪存支持，AX3600 主要使用 eMMC，但平台可能也支持 NAND)
CONFIG_ARCH_64BIT=y
# (目标架构为 64 位)
CONFIG_VIRTIO_SUPPORT=y
# (启用 VirtIO 支持，通常用于虚拟化环境)
CONFIG_aarch64=y
# (目标架构为 aarch64)
CONFIG_ARCH="aarch64"
# (目标架构名称)

#
# Target Images (目标镜像选项)
#
CONFIG_TARGET_ROOTFS_INITRAMFS=y
# (生成包含 initramfs 的根文件系统镜像)
# CONFIG_TARGET_INITRAMFS_COMPRESSION_NONE is not set
# CONFIG_TARGET_INITRAMFS_COMPRESSION_GZIP is not set
# CONFIG_TARGET_INITRAMFS_COMPRESSION_BZIP2 is not set
# CONFIG_TARGET_INITRAMFS_COMPRESSION_LZMA is not set
# CONFIG_TARGET_INITRAMFS_COMPRESSION_LZO is not set
# CONFIG_TARGET_INITRAMFS_COMPRESSION_LZ4 is not set
# CONFIG_TARGET_INITRAMFS_COMPRESSION_XZ is not set
CONFIG_TARGET_INITRAMFS_COMPRESSION_ZSTD=y
# (initramfs 使用 ZSTD 压缩算法)
CONFIG_EXTERNAL_CPIO=""
# (外部 CPIO 归档路径，此处为空)
# CONFIG_TARGET_INITRAMFS_FORCE is not set
# (不强制使用 initramfs)

#
# Root filesystem archives (根文件系统归档)
#
# CONFIG_TARGET_ROOTFS_CPIOGZ is not set
# (不生成 cpio.gz 格式的根文件系统归档)
# CONFIG_TARGET_ROOTFS_TARGZ is not set
# (不生成 tar.gz 格式的根文件系统归档)

#
# Root filesystem images (根文件系统镜像)
#
# CONFIG_TARGET_ROOTFS_EXT4FS is not set
# (不生成 ext4 格式的根文件系统镜像)
CONFIG_TARGET_ROOTFS_SQUASHFS=y
# (生成 SquashFS 格式的根文件系统镜像)
CONFIG_TARGET_SQUASHFS_BLOCK_SIZE=256
# (SquashFS 块大小为 256KB)
CONFIG_TARGET_UBIFS_FREE_SPACE_FIXUP=y
# (为 UBIFS 修复可用空间)
CONFIG_TARGET_UBIFS_JOURNAL_SIZE=""
# (UBIFS 日志大小，此处为空则使用默认值)

#
# Image Options (镜像选项)
#
# CONFIG_TARGET_ROOTFS_PERSIST_VAR is not set
# (/var 目录内容在重启后不保留)
# end of Target Images

CONFIG_EXPERIMENTAL=y
# (启用实验性功能/软件包的编译)

#
# Global build settings (全局编译设置)
#
CONFIG_JSON_OVERVIEW_IMAGE_INFO=y
# (生成 JSON 格式的镜像信息概览)
# CONFIG_JSON_CYCLONEDX_SBOM is not set
# (不生成 CycloneDX SBOM 格式的软件物料清单)
# CONFIG_ALL_NONSHARED is not set
# (不将所有库编译为静态库)
# CONFIG_ALL_KMODS is not set
# (不编译所有可用的内核模块)
# CONFIG_ALL is not set
# (不编译所有可用的软件包)
# CONFIG_BUILDBOT is not set
# (不是为 buildbot 环境编译)
CONFIG_SIGNED_PACKAGES=y
# (对软件包进行签名)
CONFIG_SIGNATURE_CHECK=y
# (检查软件包签名)
CONFIG_DOWNLOAD_CHECK_CERTIFICATE=y
# (下载时检查证书)
CONFIG_USE_APK=y
# (使用 apk 作为包管理器格式，OpenWrt 通常使用 opkg，这可能是 Alpine Linux 的影响或特定配置)

#
# General build options (通用编译选项)
#
# CONFIG_DISPLAY_SUPPORT is not set
# (不启用显示支持)
# CONFIG_BUILD_PATENTED is not set
# (不编译受专利保护的软件)
# CONFIG_BUILD_NLS is not set
# (不编译本地语言支持 NLS)
CONFIG_SHADOW_PASSWORDS=y
# (使用 shadow 密码)
# CONFIG_CLEAN_IPKG is not set
# (不清理 ipkg 文件)
# CONFIG_INCLUDE_CONFIG is not set
# (不将 .config 文件包含在固件中)
# CONFIG_REPRODUCIBLE_DEBUG_INFO is not set
# (不生成可复现的调试信息)
# CONFIG_COLLECT_KERNEL_DEBUG is not set
# (不收集内核调试信息)

#
# Kernel build options (内核编译选项)
#
CONFIG_KERNEL_BUILD_USER=""
# (内核编译用户名，为空则自动检测)
CONFIG_KERNEL_BUILD_DOMAIN=""
# (内核编译域名，为空则自动检测)
CONFIG_KERNEL_PRINTK=y
# (启用内核 printk 输出)
CONFIG_KERNEL_SWAP=y
# (启用内核交换空间支持)
# CONFIG_KERNEL_PROC_STRIPPED is not set
# (不剥离 /proc 文件系统中的符号信息)
CONFIG_KERNEL_DEBUG_FS=y
# (启用内核 debugfs 文件系统)
# CONFIG_KERNEL_PERF_EVENTS is not set
# (不启用内核性能事件)
# CONFIG_KERNEL_PROFILING is not set
# (不启用内核分析)
# CONFIG_KERNEL_UBSAN is not set
# (不启用内核 Undefined Behavior Sanitizer)
# CONFIG_KERNEL_KASAN is not set
# (不启用内核 AddressSanitizer)
# CONFIG_KERNEL_KCOV is not set
# (不启用内核代码覆盖率)
# CONFIG_KERNEL_TASKSTATS is not set
# (不启用任务统计)
# CONFIG_KERNEL_PSI is not set
# (不启用压力停顿信息 Pressure Stall Information)
CONFIG_KERNEL_KALLSYMS=y
# (启用内核 kallsyms，允许查找内核符号)
# CONFIG_KERNEL_FTRACE is not set
# (不启用内核 ftrace 函数跟踪)
CONFIG_KERNEL_DEBUG_KERNEL=y
# (启用内核调试)
CONFIG_KERNEL_DEBUG_INFO=y
# (包含内核调试信息)
CONFIG_KERNEL_DEBUG_INFO_REDUCED=y
# (减少内核调试信息量)
CONFIG_KERNEL_FRAME_WARN=2048
# (内核栈帧警告阈值，2048 字节)
# CONFIG_KERNEL_DEBUG_VIRTUAL is not set
# (不启用虚拟地址调试)
# CONFIG_KERNEL_DYNAMIC_DEBUG is not set
# (不启用动态调试)
# CONFIG_KERNEL_KPROBES is not set
# (不启用 kprobes)
# CONFIG_KERNEL_BPF_EVENTS is not set
# (不启用 BPF 事件)
CONFIG_KERNEL_AIO=y
# (启用异步 I/O)
CONFIG_KERNEL_IO_URING=y
# (启用 io_uring 异步 I/O 接口)
CONFIG_KERNEL_FHANDLE=y
# (启用文件句柄)
CONFIG_KERNEL_FANOTIFY=y
# (启用 fanotify 文件系统事件通知)
# CONFIG_KERNEL_BLK_DEV_BSG is not set
# (不启用块设备通用 SCSI 命令支持)
# CONFIG_KERNEL_HUGETLB_PAGE is not set
# (不启用大页支持)
CONFIG_KERNEL_MAGIC_SYSRQ=y
# (启用 Magic SysRq 键)
# CONFIG_KERNEL_DEBUG_PINCTRL is not set
# (不启用 pinctrl 调试)
# CONFIG_KERNEL_DEBUG_GPIO is not set
# (不启用 GPIO 调试)
CONFIG_KERNEL_COREDUMP=y
# (启用内核 coredump)
CONFIG_KERNEL_ELF_CORE=y
# (生成 ELF 格式的 coredump)
# CONFIG_KERNEL_PROVE_LOCKING is not set
# (不启用锁证明)
# CONFIG_KERNEL_SOFTLOCKUP_DETECTOR is not set
# (不启用软死锁检测器)
# CONFIG_KERNEL_HARDLOCKUP_DETECTOR is not set
# (不启用硬死锁检测器)
# CONFIG_KERNEL_DETECT_HUNG_TASK is not set
# (不启用挂起任务检测)
# CONFIG_KERNEL_WQ_WATCHDOG is not set
# (不启用工作队列看门狗)
# CONFIG_KERNEL_DEBUG_ATOMIC_SLEEP is not set
# (不启用原子操作睡眠调试)
# CONFIG_KERNEL_DEBUG_VM is not set
# (不启用 VM 调试)
CONFIG_KERNEL_PRINTK_TIME=y
# (printk 输出包含时间戳)
# CONFIG_KERNEL_SLUB_DEBUG is not set
# (不启用 SLUB 分配器调试)
# CONFIG_KERNEL_SLABINFO is not set
# (不启用 slabinfo)
# CONFIG_KERNEL_PROC_PAGE_MONITOR is not set
# (不启用 /proc/pagemonitor)
# CONFIG_KERNEL_KEXEC is not set
# (不启用 kexec，用于从当前运行的内核启动另一个内核)
# CONFIG_USE_RFKILL is not set
# (不使用 rfkill，用于禁用无线设备)
# CONFIG_USE_SPARSE is not set
# (不使用 sparse 进行静态代码分析)
# CONFIG_KERNEL_DEVTMPFS is not set
# (不使用 devtmpfs，如果未设置，则使用传统的 devfs 或 udev)
CONFIG_KERNEL_KEYS=y
# (启用内核密钥管理)
# CONFIG_KERNEL_PERSISTENT_KEYRINGS is not set
# (不启用持久密钥环)
# CONFIG_KERNEL_KEYS_REQUEST_CACHE is not set
# (不启用密钥请求缓存)
# CONFIG_KERNEL_BIG_KEYS is not set
# (不启用大密钥类型)
CONFIG_KERNEL_CGROUPS=y
# (启用控制组 cgroups)
# CONFIG_KERNEL_CGROUP_DEBUG is not set
# (不启用 cgroup 调试)
CONFIG_KERNEL_FREEZER=y
# (启用 cgroup freezer 子系统)
# CONFIG_KERNEL_CGROUP_FREEZER is not set
# (这是旧的 freezer cgroup 选项)
# CONFIG_KERNEL_CGROUP_DEVICE is not set
# (不启用 cgroup device 子系统)
# CONFIG_KERNEL_CGROUP_HUGETLB is not set
# (不启用 cgroup hugetlb 子系统)
CONFIG_KERNEL_CGROUP_PIDS=y
# (启用 cgroup pids 子系统，限制进程数量)
CONFIG_KERNEL_CGROUP_RDMA=y
# (启用 cgroup RDMA 子系统)
CONFIG_KERNEL_CGROUP_BPF=y
# (启用 cgroup BPF 支持)
CONFIG_KERNEL_CPUSETS=y
# (启用 cgroup cpusets 子系统，分配 CPU 和内存节点)
# CONFIG_KERNEL_CPUSETS_V1 is not set
# (不启用 cpusets v1 接口)
# CONFIG_KERNEL_PROC_PID_CPUSET is not set
# (不在 /proc/[pid]/cpuset 显示信息)
CONFIG_KERNEL_CGROUP_CPUACCT=y
# (启用 cgroup cpuacct 子系统，统计 CPU 使用时间)
CONFIG_KERNEL_RESOURCE_COUNTERS=y
# (启用资源计数器)
CONFIG_KERNEL_MM_OWNER=y
# (跟踪内存区域所有者)
CONFIG_KERNEL_MEMCG=y
# (启用 cgroup memory 子系统，限制内存使用)
CONFIG_KERNEL_MEMCG_SWAP=y
# (允许 memory cgroup 使用交换空间)
# CONFIG_KERNEL_MEMCG_SWAP_ENABLED is not set
# (默认不启用 memory cgroup 交换)
CONFIG_KERNEL_MEMCG_KMEM=y
# (启用 memory cgroup 内核内存记账)
# CONFIG_KERNEL_MEMCG_V1 is not set
# (不启用 memory cgroup v1 接口)
# CONFIG_KERNEL_CGROUP_PERF is not set
# (不启用 cgroup perf_event 子系统)
CONFIG_KERNEL_CGROUP_SCHED=y
# (启用 cgroup sched 子系统，控制 CPU 调度)
CONFIG_KERNEL_FAIR_GROUP_SCHED=y
# (启用公平组调度)
CONFIG_KERNEL_CFS_BANDWIDTH=y
# (启用 CFS 调度器的带宽控制)
CONFIG_KERNEL_RT_GROUP_SCHED=y
# (启用实时组调度)
CONFIG_KERNEL_BLK_CGROUP=y
# (启用 cgroup blkio 子系统，控制块设备 I/O)
# CONFIG_KERNEL_CFQ_GROUP_IOSCHED is not set
# (不使用 CFQ I/O 调度器的组调度)
CONFIG_KERNEL_BLK_DEV_THROTTLING=y
# (启用块设备 I/O 限流)
# CONFIG_KERNEL_BLK_DEV_THROTTLING_LOW is not set
# (不启用低延迟块设备 I/O 限流)
# CONFIG_KERNEL_DEBUG_BLK_CGROUP is not set
# (不启用 blkcg 调试)
# CONFIG_KERNEL_NET_CLS_CGROUP is not set
# (不启用 cgroup network classifier 子系统)
# CONFIG_KERNEL_CGROUP_NET_CLASSID is not set
# (不启用 cgroup network classid)
# CONFIG_KERNEL_CGROUP_NET_PRIO is not set
# (不启用 cgroup network priority)
CONFIG_KERNEL_NAMESPACES=y
# (启用内核命名空间)
CONFIG_KERNEL_UTS_NS=y
# (启用 UTS 命名空间，隔离主机名和域名)
CONFIG_KERNEL_IPC_NS=y
# (启用 IPC 命名空间，隔离进程间通信)
CONFIG_KERNEL_USER_NS=y
# (启用用户命名空间，隔离用户和组 ID)
CONFIG_KERNEL_PID_NS=y
# (启用 PID 命名空间，隔离进程 ID)
CONFIG_KERNEL_NET_NS=y
# (启用网络命名空间，隔离网络栈)
CONFIG_KERNEL_DEVPTS_MULTIPLE_INSTANCES=y
# (支持多个 devpts 文件系统实例)
CONFIG_KERNEL_POSIX_MQUEUE=y
# (启用 POSIX 消息队列)
CONFIG_KERNEL_SECCOMP_FILTER=y
# (启用 seccomp BPF 沙箱过滤器)
CONFIG_KERNEL_SECCOMP=y
# (启用 seccomp 安全计算模式)
CONFIG_KERNEL_IP_MROUTE=y
# (启用 IP 组播路由)
CONFIG_KERNEL_IP_MROUTE_MULTIPLE_TABLES=y
# (支持多个 IP 组播路由表)
CONFIG_KERNEL_IP_PIMSM_V1=y
# (启用 PIM-SM v1 (稀疏模式协议无关组播))
CONFIG_KERNEL_IP_PIMSM_V2=y
# (启用 PIM-SM v2)
CONFIG_KERNEL_IPV6=y
# (启用 IPv6 支持)
CONFIG_KERNEL_IPV6_MULTIPLE_TABLES=y
# (支持多个 IPv6 路由表)
CONFIG_KERNEL_IPV6_SUBTREES=y
# (启用 IPv6 路由子树优化)
CONFIG_KERNEL_IPV6_MROUTE=y
# (启用 IPv6 组播路由)
CONFIG_KERNEL_IPV6_MROUTE_MULTIPLE_TABLES=y
# (支持多个 IPv6 组播路由表)
CONFIG_KERNEL_IPV6_PIMSM_V2=y
# (启用 IPv6 PIM-SM v2)
CONFIG_KERNEL_IPV6_SEG6_LWTUNNEL=y
# (启用 IPv6 Segment Routing (SRv6) LWTunnel)
# CONFIG_KERNEL_LWTUNNEL_BPF is not set
# (不启用 LWTunnel BPF 支持)
CONFIG_KERNEL_NET_L3_MASTER_DEV=y
# (启用 L3 主设备支持，用于 VRF 等)
# CONFIG_KERNEL_DCB is not set
# (不启用数据中心桥接 DCB)
# CONFIG_KERNEL_XDP_SOCKETS is not set
# (不启用 XDP 套接字)
# CONFIG_KERNEL_PAGE_POOL is not set
# (不启用 page pool API，用于网络驱动)
CONFIG_KERNEL_MPTCP=y
# (启用 Multipath TCP)
CONFIG_KERNEL_MPTCP_IPV6=y
# (启用 MPTCP 的 IPv6 支持)
CONFIG_KERNEL_NF_CONNTRACK_TIMEOUT=y
# (启用 netfilter 连接跟踪超时调整)
# CONFIG_KERNEL_IP_PNP is not set
# (不启用 IP 即插即用协议，如 BOOTP, DHCP, RARP)
# CONFIG_KERNEL_BTRFS_FS is not set
# (不编译 Btrfs 文件系统支持)

#
# Filesystem ACL and attr support options (文件系统 ACL 和扩展属性支持选项)
#
CONFIG_USE_FS_ACL_ATTR=y
# (使用文件系统 ACL 和扩展属性)
CONFIG_KERNEL_FS_POSIX_ACL=y
# (启用 POSIX ACL 支持)
CONFIG_KERNEL_BTRFS_FS_POSIX_ACL=y
# (Btrfs 文件系统 POSIX ACL 支持)
CONFIG_KERNEL_EXT4_FS_POSIX_ACL=y
# (ext4 文件系统 POSIX ACL 支持)
CONFIG_KERNEL_F2FS_FS_POSIX_ACL=y
# (F2FS 文件系统 POSIX ACL 支持)
CONFIG_KERNEL_JFFS2_FS_POSIX_ACL=y
# (JFFS2 文件系统 POSIX ACL 支持)
CONFIG_KERNEL_TMPFS_POSIX_ACL=y
# (tmpfs 文件系统 POSIX ACL 支持)
CONFIG_KERNEL_CIFS_ACL=y
# (CIFS/SMB 文件系统 ACL 支持)
CONFIG_KERNEL_HFS_FS_POSIX_ACL=y
# (HFS 文件系统 POSIX ACL 支持)
CONFIG_KERNEL_HFSPLUS_FS_POSIX_ACL=y
# (HFS+ 文件系统 POSIX ACL 支持)
CONFIG_KERNEL_NFS_ACL_SUPPORT=y
# (NFS 文件系统 ACL 支持)
# CONFIG_KERNEL_NFS_V3_ACL_SUPPORT is not set
# (不启用 NFSv3 ACL 支持)
# CONFIG_KERNEL_NFSD_V2_ACL_SUPPORT is not set
# (不启用 NFSd v2 ACL 支持)
# CONFIG_KERNEL_NFSD_V3_ACL_SUPPORT is not set
# (不启用 NFSd v3 ACL 支持)
CONFIG_KERNEL_REISER_FS_POSIX_ACL=y
# (ReiserFS 文件系统 POSIX ACL 支持)
CONFIG_KERNEL_XFS_POSIX_ACL=y
# (XFS 文件系统 POSIX ACL 支持)
CONFIG_KERNEL_JFS_POSIX_ACL=y
# (JFS 文件系统 POSIX ACL 支持)
# end of Filesystem ACL and attr support options

# CONFIG_KERNEL_DEVMEM is not set
# (不启用 /dev/mem 设备，允许直接访问物理内存)
# CONFIG_KERNEL_DEVKMEM is not set
# (不启用 /dev/kmem 设备，允许直接访问内核内存)
CONFIG_KERNEL_SQUASHFS_FRAGMENT_CACHE_SIZE=3
# (SquashFS 碎片缓存大小，3 个片段)
# CONFIG_KERNEL_SQUASHFS_XATTR is not set
# (不启用 SquashFS 扩展属性支持)
CONFIG_KERNEL_CC_OPTIMIZE_FOR_PERFORMANCE=y
# (内核编译优化：优先性能)
# CONFIG_KERNEL_CC_OPTIMIZE_FOR_SIZE is not set
# (内核编译优化：不优先大小)
# CONFIG_KERNEL_AUDIT is not set
# (不启用内核审计子系统)
# CONFIG_KERNEL_SECURITY is not set
# (不启用额外的安全模块框架，如 AppArmor, SMACK)
# CONFIG_KERNEL_SECURITY_NETWORK is not set
# (不启用网络安全模块)
# CONFIG_KERNEL_SECURITY_SELINUX is not set
# (不启用 SELinux)
CONFIG_KERNEL_EXT4_FS_SECURITY=y
# (ext4 文件系统安全标签支持)
CONFIG_KERNEL_F2FS_FS_SECURITY=y
# (F2FS 文件系统安全标签支持)
CONFIG_KERNEL_UBIFS_FS_SECURITY=y
# (UBIFS 文件系统安全标签支持)
CONFIG_KERNEL_JFFS2_FS_SECURITY=y
# (JFFS2 文件系统安全标签支持)
CONFIG_KERNEL_WERROR=y
# (将内核编译警告视为错误)
# end of Kernel build options

#
# Package build options (软件包编译选项)
#
# CONFIG_DEBUG is not set
# (不编译调试版本的软件包)
# CONFIG_USE_GC_SECTIONS is not set
# (不使用链接器垃圾回收未使用的代码段和数据段)
# CONFIG_USE_LTO is not set
# (不使用链接时优化 Link Time Optimization)
CONFIG_MOLD=y
# (启用 mold 链接器支持，但下面未启用)
# CONFIG_USE_MOLD is not set
# (不使用 mold 作为默认链接器)
CONFIG_IPV6=y
# (启用 IPv6 支持 (软件包层面))

#
# Stripping options (剥离选项，减小二进制文件大小)
#
# CONFIG_NO_STRIP is not set
# (不禁用剥离)
# CONFIG_USE_STRIP is not set
# (不使用标准的 strip 工具)
CONFIG_USE_SSTRIP=y
# (使用 sstrip 工具进行更积极的剥离)
CONFIG_SSTRIP_DISCARD_TRAILING_ZEROES=y
# (sstrip 丢弃尾随的零)
# CONFIG_USE_MKLIBS is not set
# (不使用 mklibs 优化共享库)

#
# Hardening build options (强化编译选项，增强安全性)
#
CONFIG_PKG_CHECK_FORMAT_SECURITY=y
# (检查格式化字符串安全问题)
# CONFIG_PKG_ASLR_PIE_NONE is not set
# (不禁用 ASLR/PIE)
CONFIG_PKG_ASLR_PIE_REGULAR=y
# (为可执行文件启用常规的地址空间布局随机化 (ASLR) 和位置无关可执行文件 (PIE))
# CONFIG_PKG_ASLR_PIE_ALL is not set
# (不为所有可执行文件和共享库启用 PIE)
# CONFIG_PKG_CC_STACKPROTECTOR_NONE is not set
# (不禁用栈保护)
CONFIG_PKG_CC_STACKPROTECTOR_REGULAR=y
# (启用常规的栈保护，防范栈溢出攻击)
# CONFIG_PKG_CC_STACKPROTECTOR_STRONG is not set
# (不使用更强的栈保护)
# CONFIG_PKG_CC_STACKPROTECTOR_ALL is not set
# (不为所有函数启用栈保护)
# CONFIG_KERNEL_CC_STACKPROTECTOR_NONE is not set
# (内核不禁用栈保护)
CONFIG_KERNEL_CC_STACKPROTECTOR_REGULAR=y
# (内核启用常规栈保护)
# CONFIG_KERNEL_CC_STACKPROTECTOR_STRONG is not set
# (内核不使用强栈保护)
CONFIG_KERNEL_STACKPROTECTOR=y
# (内核启用栈保护)
# CONFIG_KERNEL_STACKPROTECTOR_STRONG is not set
# (内核不使用强栈保护)
# CONFIG_PKG_FORTIFY_SOURCE_NONE is not set
# (不禁用 FORTIFY_SOURCE)
CONFIG_PKG_FORTIFY_SOURCE_1=y
# (启用 FORTIFY_SOURCE 等级 1，在编译时检查一些缓冲区溢出等问题)
# CONFIG_PKG_FORTIFY_SOURCE_2 is not set
# (不使用 FORTIFY_SOURCE 等级 2，更严格但可能影响性能)
# CONFIG_PKG_RELRO_NONE is not set
# (不禁用 RELRO)
# CONFIG_PKG_RELRO_PARTIAL is not set
# (不使用部分 RELRO)
CONFIG_PKG_RELRO_FULL=y
# (启用完全 RELRO (Relocation Read-Only)，将 GOT 表等标记为只读，增强安全性)
# CONFIG_SELINUX is not set
# (不启用 SELinux (软件包层面))
CONFIG_USE_SECCOMP=y
# (使用 seccomp BPF (软件包层面))
# end of Global build settings

CONFIG_DEVEL=y
# (启用开发者选项，通常包含更多调试工具和符号信息)
# CONFIG_BROKEN is not set
# (不编译标记为损坏的软件包)
CONFIG_BINARY_FOLDER=""
# (二进制文件输出文件夹，为空则使用默认)
CONFIG_DOWNLOAD_TOOL_CUSTOM=""
# (自定义下载工具路径，为空则使用默认)
CONFIG_DOWNLOAD_FOLDER=""
# (下载缓存文件夹，为空则使用默认 `dl`)
CONFIG_LOCALMIRROR=""
# (本地镜像地址，为空则不使用)
CONFIG_AUTOREBUILD=y
# (当依赖项更新时自动重新编译)
# CONFIG_AUTOREMOVE is not set
# (不自动移除过时的软件包)
# CONFIG_BUILD_ALL_HOST_TOOLS is not set
# (不编译所有宿主机工具)
# CONFIG_OPTIMIZE_HOST_TOOLS is not set
# (不优化宿主机工具的编译)
CONFIG_BUILD_SUFFIX=""
# (编译后缀，为空则无后缀)
CONFIG_TARGET_ROOTFS_DIR=""
# (目标根文件系统目录，为空则在 build_dir 下生成)
# CONFIG_CCACHE is not set
# (不使用 ccache 编译缓存)
CONFIG_CCACHE_DIR=""
# (ccache 目录，为空则使用默认)
CONFIG_KERNEL_CFLAGS=""
# (额外的内核编译参数)
CONFIG_EXTERNAL_KERNEL_TREE=""
# (外部内核源码树路径)
CONFIG_KERNEL_GIT_CLONE_URI=""
# (内核 Git 克隆地址)
# CONFIG_BUILD_LOG is not set
# (不保存详细的编译日志)
CONFIG_BUILD_LOG_DIR=""
# (编译日志目录)
# CONFIG_SRC_TREE_OVERRIDE is not set
# (不覆盖源码树)
CONFIG_EXTRA_OPTIMIZATION="-fno-caller-saves -fno-plt"
# (额外的优化选项：-fno-caller-saves 避免函数调用时保存和恢复所有寄存器，-fno-plt 避免使用 PLT 表，可能提高性能但增大体积)
# CONFIG_TARGET_OPTIONS is not set
# (目标特定选项未启用)
CONFIG_TARGET_OPTIMIZATION="-Os -pipe -mcpu=cortex-a53"
# (目标优化选项，与前面默认值一致)
# CONFIG_BPF_TOOLCHAIN_NONE is not set
# (不禁用 BPF 工具链)
# CONFIG_BPF_TOOLCHAIN_HOST is not set
# (不使用宿主机的 BPF 工具链)
CONFIG_BPF_TOOLCHAIN_BUILD_LLVM=y
# (编译 LLVM 作为 BPF 工具链)
# CONFIG_EXTERNAL_TOOLCHAIN is not set
# (不使用外部工具链)
CONFIG_NEED_TOOLCHAIN=y
# (需要编译工具链)
# CONFIG_TOOLCHAINOPTS is not set
# (工具链选项未启用)
CONFIG_EXTRA_BINUTILS_CONFIG_OPTIONS=""
# (额外的 binutils 配置选项)
CONFIG_EXTRA_GCC_CONFIG_OPTIONS=""
# (额外的 GCC 配置选项)
CONFIG_MUSL_DISABLE_CRYPT_SIZE_HACK=y
# (Musl C 库：禁用 crypt() 大小相关的 hack)
CONFIG_GDB=y
# (编译 GDB 调试器)
# CONFIG_GDB_PYTHON is not set
# (不编译 GDB 的 Python 支持)
CONFIG_HAS_BPF_TOOLCHAIN=y
# (系统有 BPF 工具链)
CONFIG_NEED_BPF_TOOLCHAIN=y
# (需要 BPF 工具链)
# CONFIG_HAS_PREBUILT_LLVM_TOOLCHAIN is not set
# (没有预编译的 LLVM 工具链)
CONFIG_USE_LLVM_BUILD=y
# (使用编译的 LLVM)
CONFIG_USE_MUSL=y
# (使用 Musl C 库)
CONFIG_SSP_SUPPORT=y
# (启用 SSP (Stack Smashing Protector) 支持)
CONFIG_BINUTILS_VERSION_2_42=y
# (binutils 版本为 2.42)
CONFIG_BINUTILS_VERSION="2.42"
# (binutils 版本字符串)
CONFIG_GCC_VERSION="14.3.0"
# (GCC 版本字符串)
CONFIG_GCC_USE_DEFAULT_VERSION=y
# (使用默认的 GCC 版本，即上面指定的 14.3.0)
CONFIG_LIBC="musl"
# (C 库为 musl)
CONFIG_TARGET_SUFFIX="musl"
# (目标后缀，通常与 C 库相关)
CONFIG_IB=y
# (启用 ImageBuilder 支持)
CONFIG_IB_STANDALONE=y
# (独立 ImageBuilder)
CONFIG_SDK=y
# (编译 SDK)
CONFIG_SDK_LLVM_BPF=y
# (SDK 包含 LLVM BPF 工具)
CONFIG_MAKE_TOOLCHAIN=y
# (生成工具链 makefile)
CONFIG_IMAGEOPT=y
# (启用镜像选项)
# CONFIG_TARGET_DEFAULT_LAN_IP_FROM_PREINIT is not set
# (不从 preinit 获取默认 LAN IP)
# CONFIG_PREINITOPT is not set
# (preinit 选项未启用)
CONFIG_TARGET_PREINIT_SUPPRESS_STDERR=y
# (preinit 阶段禁止标准错误输出)
# CONFIG_TARGET_PREINIT_DISABLE_FAILSAFE is not set
# (不禁用 preinit 的 failsafe 模式)
CONFIG_TARGET_PREINIT_TIMEOUT=4
# (preinit 超时时间为 4 秒)
# CONFIG_TARGET_PREINIT_SHOW_NETMSG is not set
# (preinit 不显示网络消息)
# CONFIG_TARGET_PREINIT_SUPPRESS_FAILSAFE_NETMSG is not set
# (不禁止 failsafe 模式的网络消息)
CONFIG_TARGET_PREINIT_IFNAME=""
# (preinit 使用的网络接口名称，为空则自动检测)
CONFIG_TARGET_PREINIT_IP="192.168.1.1"
# (preinit 使用的 IP 地址)
CONFIG_TARGET_PREINIT_NETMASK="255.255.255.0"
# (preinit 使用的子网掩码)
CONFIG_TARGET_PREINIT_BROADCAST="192.168.1.255"
# (preinit 使用的广播地址)
# CONFIG_INITOPT is not set
# (init 选项未启用)
CONFIG_TARGET_INIT_PATH="/usr/sbin:/usr/bin:/sbin:/bin"
# (init 进程的 PATH 环境变量)
CONFIG_TARGET_INIT_ENV=""
# (init 进程的额外环境变量)
CONFIG_TARGET_INIT_CMD="/sbin/init"
# (init 进程的命令路径)
CONFIG_TARGET_INIT_SUPPRESS_STDERR=y
# (init 阶段禁止标准错误输出)
# CONFIG_VERSIONOPT is not set
# (版本选项未启用)
# CONFIG_PER_FEED_REPO is not set
# (不为每个 feed 创建单独的仓库)

#
# Base system (基础系统软件包)
#
CONFIG_PACKAGE_apk-mbedtls=y
# (包含 apk 包管理器，使用 mbedtls)
# CONFIG_PACKAGE_apk-openssl is not set
# (不包含使用 openssl 的 apk)
CONFIG_PACKAGE_base-files=y
# (包含 base-files 软件包，提供基础目录结构和文件)
# CONFIG_PACKAGE_block-mount is not set
# (不包含 block-mount，用于挂载块设备)
# CONFIG_PACKAGE_blockd is not set
# (不包含 blockd，块设备事件守护进程)
# CONFIG_PACKAGE_bridger is not set
# (不包含 bridger，用于简化网桥配置)
CONFIG_PACKAGE_busybox=y
# (包含 busybox，集成了许多常用 Linux 工具的单个可执行文件)
# CONFIG_BUSYBOX_CUSTOM is not set
# (不使用自定义的 busybox 配置)
CONFIG_BUSYBOX_DEFAULT_HAVE_DOT_CONFIG=y
# (busybox 配置：存在 .config 文件)
# CONFIG_BUSYBOX_DEFAULT_DESKTOP is not set
# (busybox 配置：不启用桌面特性)
# CONFIG_BUSYBOX_DEFAULT_EXTRA_COMPAT is not set
# (busybox 配置：不启用额外的兼容性特性)
# CONFIG_BUSYBOX_DEFAULT_FEDORA_COMPAT is not set
# (busybox 配置：不启用 Fedora 兼容性)
CONFIG_BUSYBOX_DEFAULT_INCLUDE_SUSv2=y
# (busybox 配置：包含 Single UNIX Specification v2 兼容性)
CONFIG_BUSYBOX_DEFAULT_LONG_OPTS=y
# (busybox 配置：启用长选项支持，如 --help)
CONFIG_BUSYBOX_DEFAULT_SHOW_USAGE=y
# (busybox 配置：显示用法信息)
CONFIG_BUSYBOX_DEFAULT_FEATURE_VERBOSE_USAGE=y
# (busybox 配置：显示详细的用法信息)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_COMPRESS_USAGE is not set
# (busybox 配置：不压缩用法信息)
CONFIG_BUSYBOX_DEFAULT_LFS=y
# (busybox 配置：启用大文件支持)
# CONFIG_BUSYBOX_DEFAULT_TIME64 is not set
# (busybox 配置：不默认使用 64 位 time_t)
# CONFIG_BUSYBOX_DEFAULT_PAM is not set
# (busybox 配置：不启用 PAM 支持)
CONFIG_BUSYBOX_DEFAULT_FEATURE_DEVPTS=y
# (busybox 配置：启用 /dev/pts 支持)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_UTMP is not set
# (busybox 配置：不启用 utmp 支持)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_WTMP is not set
# (busybox 配置：不启用 wtmp 支持)
CONFIG_BUSYBOX_DEFAULT_FEATURE_PIDFILE=y
# (busybox 配置：启用 PID 文件支持)
CONFIG_BUSYBOX_DEFAULT_PID_FILE_PATH="/var/run"
# (busybox 配置：PID 文件路径)
# CONFIG_BUSYBOX_DEFAULT_BUSYBOX is not set
# (busybox 配置：不编译 busybox applet 本身)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_SHOW_SCRIPT is not set
# (busybox 配置：不显示脚本内容)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_INSTALLER is not set
# (busybox 配置：不启用安装器特性)
# CONFIG_BUSYBOX_DEFAULT_INSTALL_NO_USR is not set
# (busybox 配置：安装到 /usr 目录)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_SUID is not set
# (busybox 配置：不启用 SUID 支持)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_SUID_CONFIG is not set
# (busybox 配置：不启用 SUID 配置)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_SUID_CONFIG_QUIET is not set
# (busybox 配置：SUID 配置不静默)
CONFIG_BUSYBOX_DEFAULT_FEATURE_PREFER_APPLETS=y
# (busybox 配置：优先使用内部 applet 而不是外部命令)
CONFIG_BUSYBOX_DEFAULT_BUSYBOX_EXEC_PATH="/proc/self/exe"
# (busybox 配置：busybox 可执行文件路径)
# CONFIG_BUSYBOX_DEFAULT_SELINUX is not set
# (busybox 配置：不启用 SELinux 支持)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_CLEAN_UP is not set
# (busybox 配置：不启用清理特性)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_SYSLOG_INFO is not set
# (busybox 配置：不启用 syslog info 级别)
CONFIG_BUSYBOX_DEFAULT_FEATURE_SYSLOG=y
# (busybox 配置：启用 syslog 支持)
# CONFIG_BUSYBOX_DEFAULT_STATIC is not set
# (busybox 配置：不静态链接)
# CONFIG_BUSYBOX_DEFAULT_PIE is not set
# (busybox 配置：不编译为 PIE)
# CONFIG_BUSYBOX_DEFAULT_NOMMU is not set
# (busybox 配置：不为无 MMU 系统编译)
# CONFIG_BUSYBOX_DEFAULT_BUILD_LIBBUSYBOX is not set
# (busybox 配置：不编译为库)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_LIBBUSYBOX_STATIC is not set
# (busybox 配置：不静态链接 libbusybox)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_INDIVIDUAL is not set
# (busybox 配置：不将 applet 编译为独立可执行文件)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_SHARED_BUSYBOX is not set
# (busybox 配置：不编译为共享库)
CONFIG_BUSYBOX_DEFAULT_CROSS_COMPILER_PREFIX=""
# (busybox 配置：交叉编译器前缀)
CONFIG_BUSYBOX_DEFAULT_SYSROOT=""
# (busybox 配置：sysroot 路径)
CONFIG_BUSYBOX_DEFAULT_EXTRA_CFLAGS=""
# (busybox 配置：额外的 CFLAGS)
CONFIG_BUSYBOX_DEFAULT_EXTRA_LDFLAGS=""
# (busybox 配置：额外的 LDFLAGS)
CONFIG_BUSYBOX_DEFAULT_EXTRA_LDLIBS=""
# (busybox 配置：额外的 LDLIBS)
# CONFIG_BUSYBOX_DEFAULT_USE_PORTABLE_CODE is not set
# (busybox 配置：不使用可移植代码)
# CONFIG_BUSYBOX_DEFAULT_STACK_OPTIMIZATION_386 is not set
# (busybox 配置：不为 386 优化栈)
# CONFIG_BUSYBOX_DEFAULT_STATIC_LIBGCC is not set
# (busybox 配置：不静态链接 libgcc)
CONFIG_BUSYBOX_DEFAULT_INSTALL_APPLET_SYMLINKS=y
# (busybox 配置：将 applet 安装为符号链接)
# CONFIG_BUSYBOX_DEFAULT_INSTALL_APPLET_HARDLINKS is not set
# (busybox 配置：不将 applet 安装为硬链接)
# CONFIG_BUSYBOX_DEFAULT_INSTALL_APPLET_SCRIPT_WRAPPERS is not set
# (busybox 配置：不为 applet 安装脚本包装器)
# CONFIG_BUSYBOX_DEFAULT_INSTALL_APPLET_DONT is not set
# (busybox 配置：安装 applet)
# CONFIG_BUSYBOX_DEFAULT_INSTALL_SH_APPLET_SYMLINK is not set
# (busybox 配置：不为 sh 安装符号链接)
# CONFIG_BUSYBOX_DEFAULT_INSTALL_SH_APPLET_HARDLINK is not set
# (busybox 配置：不为 sh 安装硬链接)
# CONFIG_BUSYBOX_DEFAULT_INSTALL_SH_APPLET_SCRIPT_WRAPPER is not set
# (busybox 配置：不为 sh 安装脚本包装器)
CONFIG_BUSYBOX_DEFAULT_PREFIX="./_install"
# (busybox 配置：安装前缀)
# CONFIG_BUSYBOX_DEFAULT_DEBUG is not set
# (busybox 配置：不启用调试)
# CONFIG_BUSYBOX_DEFAULT_DEBUG_PESSIMIZE is not set
# (busybox 配置：不进行悲观调试优化)
# CONFIG_BUSYBOX_DEFAULT_DEBUG_SANITIZE is not set
# (busybox 配置：不启用 sanitize 调试)
# CONFIG_BUSYBOX_DEFAULT_UNIT_TEST is not set
# (busybox 配置：不编译单元测试)
# CONFIG_BUSYBOX_DEFAULT_WERROR is not set
# (busybox 配置：不将警告视为错误)
# CONFIG_BUSYBOX_DEFAULT_WARN_SIMPLE_MSG is not set
# (busybox 配置：不使用简单警告消息)
CONFIG_BUSYBOX_DEFAULT_NO_DEBUG_LIB=y
# (busybox 配置：不链接调试库)
# CONFIG_BUSYBOX_DEFAULT_DMALLOC is not set
# (busybox 配置：不使用 dmalloc)
# CONFIG_BUSYBOX_DEFAULT_EFENCE is not set
# (busybox 配置：不使用 Electric Fence)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_USE_BSS_TAIL is not set
# (busybox 配置：不使用 BSS 段尾部优化)
# CONFIG_BUSYBOX_DEFAULT_FLOAT_DURATION is not set
# (busybox 配置：时间不使用浮点数表示)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_RTMINMAX is not set
# (busybox 配置：不启用 rt_minmax)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_RTMINMAX_USE_LIBC_DEFINITIONS is not set
# (busybox 配置：rt_minmax 不使用 libc 定义)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_BUFFERS_USE_MALLOC is not set
# (busybox 配置：缓冲区不使用 malloc 分配)
CONFIG_BUSYBOX_DEFAULT_FEATURE_BUFFERS_GO_ON_STACK=y
# (busybox 配置：缓冲区在栈上分配)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_BUFFERS_GO_IN_BSS is not set
# (busybox 配置：缓冲区不在 BSS 段分配)
CONFIG_BUSYBOX_DEFAULT_PASSWORD_MINLEN=6
# (busybox 配置：密码最小长度为 6)
CONFIG_BUSYBOX_DEFAULT_MD5_SMALL=1
# (busybox 配置：MD5 实现大小优化等级 1)
CONFIG_BUSYBOX_DEFAULT_SHA1_SMALL=3
# (busybox 配置：SHA1 实现大小优化等级 3)
CONFIG_BUSYBOX_DEFAULT_SHA1_HWACCEL=y
# (busybox 配置：启用 SHA1 硬件加速)
CONFIG_BUSYBOX_DEFAULT_SHA256_HWACCEL=y
# (busybox 配置：启用 SHA256 硬件加速)
CONFIG_BUSYBOX_DEFAULT_SHA3_SMALL=1
# (busybox 配置：SHA3 实现大小优化等级 1)
CONFIG_BUSYBOX_DEFAULT_FEATURE_NON_POSIX_CP=y
# (busybox 配置：cp 命令启用非 POSIX 特性)
# CONFIG_BUSYBOX_DEFAULT_FEATURE_VERBOSE_CP_MESSAGE is not set
# (busybox 配置：cp 命令不显示详细消息)
CONFIG_BUSYBOX_DEFAULT_FEATURE_USE_SENDFILE=y
# (busybox 配置：启用 sendfile 系统调用优化文件复制)
CONFIG_BUSYBOX_DEFAULT_FEATURE_COPYBUF_KB=4
# (busybox 配置：复制缓冲区大小为 4KB)
CONFIG_BUSYBOX_DEFAULT_MONOTONIC_SYSCALL=y
# (busybox 配置：使用 monotonic 时钟系统调用)
CONFIG_BUSYBOX_DEFAULT_IOCTL_HEX2STR_ERROR=y
# (busybox 配置：ioctl 十六进制转字符串错误处理)
CONFIG_BUSYBOX_DEFAULT_FEATURE_EDITING=y
# (busybox 配置：启用行编辑功能)

# ... (后续还有大量 busybox 和其他软件包的配置，此处省略)

# end of Utilities
# (实用工具配置结束)
